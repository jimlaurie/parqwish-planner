export const metadata = {
  title: "Updates — ParQwish",
  description: "What's new in ParQ Wish Pal and ParQ Wish Planner, roughly as often as there's something to say.",
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
